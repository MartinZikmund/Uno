<Page
	x:Class="SamplesApp.MainPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:Uno.UI.Samples.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="4">
        <TextBox x:Name="dummyText" TabIndex="0" Width="50"/>
        <TextBox	x:Name="textValue" Width="50"  Text="Cat"
						LostFocus="OnTextLostFocus" BeforeTextChanging="OnBeforeTextChanging"
						BorderBrush="Black" BorderThickness="1" TabIndex="1" GotFocus="OnTextGotFocus">
            <TextBox.Style>
                <Style TargetType="TextBox" >
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TextBox">
                                <Grid VerticalAlignment="Center" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" >
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Disabled">
                                                <Storyboard>
                                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ContentElement" Storyboard.TargetProperty="Foreground">
                                                        <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SystemControlDisabledChromeDisabledLowBrush}" />
                                                    </ObjectAnimationUsingKeyFrames>
                                                </Storyboard>
                                            </VisualState>
                                            <VisualState x:Name="Normal"/>
                                            <VisualState x:Name="PointerOver"/>
                                            <VisualState x:Name="Focused"/>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                    <ContentControl x:Name="ContentElement"
														Background="Transparent"
														Margin="{TemplateBinding BorderThickness}"
														Padding="{TemplateBinding Padding}"
														IsTabStop="False"
														AutomationProperties.AccessibilityView="Raw"
														VerticalAlignment="Stretch"
														HorizontalContentAlignment="Stretch"
														HorizontalAlignment="Stretch"
														VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TextBox.Style>
            <FlyoutBase.AttachedFlyout>
				<Flyout x:Name="flyout" Placement="BottomEdgeAlignedLeft" AllowFocusOnInteraction="False">
                    <Grid x:Name="flyoutGrid" RowSpacing="4">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Button GotFocus="OnBtnGotFocus" x:Name="InheritTest" Click="InheritTestClick" Grid.Row="4">adsaf</Button>
						<Button Content="Cat"		Grid.Row="0" Grid.Column="0" Click="OnFlyoutButtonClick" />
						<Button Content="Dog"		Grid.Row="1" Grid.Column="0" Click="OnFlyoutButtonClick" />
						<Button Content="Mouse"		Grid.Row="2" Grid.Column="0" Click="OnFlyoutButtonClick" />
                    </Grid>
                </Flyout>
            </FlyoutBase.AttachedFlyout>
        </TextBox>
    </StackPanel>
</Page>
